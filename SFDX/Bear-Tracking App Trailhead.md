#### Communication with Components across the App
- fireEvent( pageRef, evetnName, payload) ex) fireEvent(this.pageREf, 'bearListUpdate', result.data);
- Component Lifecycle hook: connectedCallback(), disconnected Callback. 
